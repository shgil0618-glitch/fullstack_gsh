package com.thejoa703.service;

import java.util.List;

import org.springframework.web.multipart.MultipartFile;

import com.thejoa703.dto.Recipes3Dto;

public interface RecipeService {

    // =======================
    // 레시피 CRUD
    // =======================
    int getTotalRecipeCount();
    List<Recipes3Dto> selectRecipeListPaging(int page);
    Recipes3Dto selectRecipeDetail(int recipeId);
    int insertRecipe(Recipes3Dto dto, List<MultipartFile> files);
    int updateRecipe(Recipes3Dto dto, List<MultipartFile> files);
    int deleteRecipe(int recipeId, int appUserId);

    void incrementViews(int recipeId);

    List<Recipes3Dto> selectMyRecipes(int appUserId);

    // =======================
    // 좋아요 기능
    // =======================
    boolean existsRecipeLike(int appUserId, int recipeId);
    boolean likeRecipe(int appUserId, int recipeId);
    boolean unlikeRecipe(int appUserId, int recipeId);

    int getAppUserIdByEmail(String email);

    // =======================
    // 페이징 DTO
    // =======================
    Object getPaging(int page, int totalCount); // 실제 PagingDto 반환
}
