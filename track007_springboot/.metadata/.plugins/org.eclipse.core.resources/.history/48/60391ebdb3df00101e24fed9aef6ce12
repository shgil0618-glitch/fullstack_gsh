package com.thejoa703.service;

import java.util.List;
import java.util.Map;

import org.springframework.web.multipart.MultipartFile;

import com.thejoa703.dto.Recipes3Dto;
import com.thejoa703.dto.RecipesIngre3;
import com.thejoa703.dto.RecipesStep3;

public interface RecipeService {

    // 레시피 CRUD
    int createRecipe(MultipartFile imageFile, Recipes3Dto dto);
    int updateRecipe(MultipartFile imageFile, Recipes3Dto dto);
    int deleteRecipe(int recipeId);
    Recipes3Dto getRecipeById(int recipeId);

    // 목록 / 검색 / 페이징
    List<Recipes3Dto> selectRecipeAllPaged(Map<String, Object> params);
    int countAll(Integer category);
    int countSearchRecipes(Map<String, Object> params);
    List<Recipes3Dto> searchRecipesPaged(Map<String, Object> params);

    // 조회수
    int incrementViews(int recipeId);

    // 재료 / 단계
    List<RecipesIngre3> getIngredients(int recipeId);
    List<RecipesStep3> getSteps(int recipeId);

    // 좋아요
    void likeRecipe(int appUserId, int recipeId);
    void unlikeRecipe(int appUserId, int recipeId);
    int countLikesByRecipe(int recipeId);

    // 검색 기록
    void saveSearchHistory(Integer appUserId, String keyword);
    List<Map<String, Object>> topKeywords(int limit);

    // 비속어 관리
    List<Map<String, Object>> getAllBadWords();
    void addBadWord(String word);
    void deleteBadWordById(int wordId);

    // AI 사용 기록 관리
    List<Map<String, Object>> getAllAiUsage();
    void deleteAiUsageById(int aiHistId);

    // 카테고리
    List<Map<String, Object>> getAllCategories();
    String getCategoryName(int category);
}
